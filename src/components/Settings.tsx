import * as React from 'react';

class Settings extends React.Component<any, any> {
    render() {
      return (
        <div className="hello">
         Hello from Settings!
        </div>
      );
    }
  }
export default Settings;